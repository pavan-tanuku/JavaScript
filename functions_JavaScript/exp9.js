// Write a function convertToUpperCase that converts all letters in a string to uppercase.
function upperCase(str) {
    let upperStr = str.toUpperCase();
    return upperStr;
}

let str = "Sasi Institute of Technology and Engineering";
console.log(upperCase(str));